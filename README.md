# KISSAN COTTAGE GULMARG — Kashmiri Products & Stays (Static Next.js)

Authentic, natural Kashmiri products and comfortable room bookings near Gulmarg. Contact via WhatsApp to order or reserve. Built with: Next.js, TypeScript, Tailwind CSS.

## Quick start

1. Install dependencies: `npm install` or `pnpm install`
2. Update your WhatsApp number: edit `lib/siteConfig.ts` and set `whatsappNumber` to your full phone (e.g. `+9199XXXXXXXX`).
3. Run dev: `npm run dev`
4. Build & export: `npm run build` (will output `out/` folder for static hosting)

## Notes
- No payment gateway is enabled — visitors contact you via WhatsApp to place orders.
- Product data is in `data/products.ts` — edit or add products there.
- Deploy to Vercel or any static host that serves the `out/` folder.

Enjoy! 👋# kissan
# kissan
